/*d*/
$$let int a=2767.3f;
let int u;
let int c;
let string ejemplo = 'ejemplo';
let boolean b = a = u; /* falta estol*/
function string cadena(int a, int b,boolean k){
	a = a + b + c;
	return 'hola';
}
ejemplo = cadena(a,u,b);
asddsa = 7;
if(ejemplo == 'hola' && a == u) c = a;
if ( a == u && c == a){
	if (b){
		input lectura;
	}
	output ((lectura));
}

function void comoEstas (boolean a,boolean c, boolean d, boolean n,string temp){
	let int jul;
	letglobal = 0;
	if (a == c == d == n && temp == ejemplo)
		output (temp);
		output 'comoEstas';
}
comoEstas(b,b,b,b,ejemplo);

let string hola;
let boolean falso = 1 == 2;

function int suma(int a,int b){
	return a + b;
}
/* hola */
function boolean and(boolean a, boolean b){
	output 'A continuacion ejecutare el and :O';
	return a && b;
}
p = suma(a,u);
let boolean ejemplo2 = and(falso,and(b,b));
and(ejemplo2,b);
let a = 23232f33

